-# index.html for biogems.info

-#= partial "requires_prerelease"
-#= content_navigation("What is Compass?", "Using Compass with StaticMatic", "Using Compass's CSS Frameworks")

#plugins
  #list
    %table
      %tr
        %th
          = "#"
        %th{ :align => "left" } biogem 
        %th{ :align => "left" } description
        %th{ :align => "left" } by
        %th{ :align => "left" }
          90d&nbsp;*)
        %th{ :align => "left" }
          total
        %th{ :align => "left" } version
      - by_popularity do |i, dl90, dl, name, version, descr, author, docs|
        - rowclass = "odd"
        - if i.even?
          - rowclass = "even"
        %tr
          %td{ :align => "right", :class => "#{rowclass}" }
            .index
              = i
          %td{ :class => "#{rowclass}" }
            .name
              = link name.gsub(/[_-]/,' '), "http://rubygems.org/gems/#{name}"
          %td{ :class => "#{rowclass}" }
            .descr
              = descr
              - if docs != 'broken'
                = link " ... ", docs
          %td{ :class => "#{rowclass}" }
            .author
              = author
          %td{ :align => "right", :class => "#{rowclass}" }
            .dl
              = dl90.to_s
          %td{ :align => "right", :class => "#{rowclass}" }
            .dl
              = link dl.to_s, "http://rubygems.org/gems/#{name}/stats"
          %td{ :align => "right", :class => "#{rowclass}" }
            .version
              = version
  .footnote 
    *) gem downloads (last 90 days/total) from 
    = link "rubygems.org", "http://rubygems.org"
    as of
    = Time.now.to_s
