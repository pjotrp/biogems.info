-# index.html for biogems.info

-#= partial "requires_prerelease"
-#= content_navigation("What is Compass?", "Using Compass with StaticMatic", "Using Compass's CSS Frameworks")

#plugins
  #list
    %table
      %tr
        %th
          = "#"
        %th{ :align => "left" } biogem 
        %th{ :align => "left" } description
        %th{ :align => "left" } by
        %th{ :align => "left" }
          dl
          %sup
            %b
              *)
      - by_popularity do |i, dl, name, descr, author|
        - rowclass = "odd"
        - if i.even?
          - rowclass = "even"
        %tr
          %td{ :align => "right", :class => "#{rowclass}" }
            .index
              = i
          %td{ :class => "#{rowclass}" }
            .name
              = link name, "http://rubygems.org/gems/#{name}"
          %td{ :class => "#{rowclass}" }
            .descr
              = descr
          %td{ :class => "#{rowclass}" }
            .author
              = author
          %td{ :align => "right", :class => "#{rowclass}" }
            .dl
              = link dl.to_s, "http://rubygems.org/gems/#{name}/stats"
  .footnote 
    *) gem downloads from 
    = link "rubygems.org", "http://rubygems.org"
    as of
    = Time.now.to_s
